// there are two tpe of functions normal function and arraow function;

// normal function

function Sum(a,b) {
   return a+b;
}
console.log(`Sum of a and b is: ${Sum(9,5)}`);

// arrow function 
const SumArrow = (x , y) => {
    return x + y;
}

console.log(`Sum of x and y is: ${SumArrow(9,8)}`);